// @flow
import {StyleSheet} from 'aphrodite';

export default StyleSheet.create({
  container: {
    height: '100vh',
    display: 'flex',
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});
